const customTheme = {
    dialog: {
        styles: {
            base: {
                backdrop: {
                    backgroundColor: "bg-black/20", // Background color
                    backgroundOpacity: "bg-opacity-20", // 20% opacity (0.2)
                    backdropBlur: "!backdrop-blur-[2px]" // Optional blur effect
                }
            }
        }
    },
};

export default customTheme;